# Docker Hub credentials for publishing images
# Required only if you want to manually publish to Docker Hub
# GitHub Actions uses repository secrets instead

# Your Docker Hub username
DOCKERHUB_USERNAME=your_dockerhub_username

# Docker Hub access token (create at https://hub.docker.com/settings/security)
DOCKERHUB_TOKEN=your_dockerhub_token
